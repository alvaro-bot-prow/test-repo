presubmits:
- name: pull-test-inrepo-hello
  always_run: true
  decorate: true
  spec:
    containers:
      - image: alpine
        command:
          - /bin/sh
          - -c
          - date && exit 1
